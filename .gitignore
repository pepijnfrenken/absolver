__pycache__/
*.pyc
.venv/
.venv/
*.egg-info/
.DS_Store

# Local pipeline output dirs — NEVER commit
abliterated_*/
checkpoints/
outputs/

# Secrets / credentials — NEVER commit
.env
.env.*
*.pem
*.key
keys/
secrets/

# Run logs
logs/

# Campaign ablated-model weights — several GB each; keep manifests/configs
# in git, keep the safetensors on disk as artifacts only
campaigns/**/model.safetensors
abl-work/
phase2-staging/
