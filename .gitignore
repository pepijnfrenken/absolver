__pycache__/
*.pyc
.venv/
.venv/
*.egg-info/
.DS_Store

# Local pipeline output dirs — NEVER commit
abliterated_*/
checkpoints/
outputs/

# Secrets / credentials — NEVER commit
.env
.env.*
*.pem
*.key
keys/
secrets/

# Run logs
logs/

# Campaign ablated-model weights — several GB each; keep manifests/configs
# in git, keep the safetensors on disk as artifacts only
campaigns/**/model.safetensors
abl-work/
phase2-staging/

# Downloaded reference artifacts (multi-GB, not repo content)
heretic-gguf/
campaigns/**/*.npz
campaigns/**/heretic-55_transcripts_*_FULL.json
campaigns/**/*_512tok_bak.json
# hf-upload: keep the provenance/config files, never the weight binary
campaigns/**/hf-upload/model.safetensors
